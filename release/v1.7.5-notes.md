## v1.7.5

### Fixed
- Fixed hiding of the Rozetka AI floating button when the host node is injected dynamically.
- Added support for both AI consultation markup variants (`rz-chat-bot-invitation` and legacy placeholder).
- Updated promo/card-price handling for the new `rz-tile-red-price` structure.
- Fixed regression where regular product price could disappear together with card price.
- Restored larger regular-price styling when card-price block is hidden.

### Validation
- Passed `npm test` (`79/79`)
- Passed `npm run test:syntax`
