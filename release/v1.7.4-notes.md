## v1.7.4

### Fixed
- Fixed a white-screen regression on Rozetka catalog pages such as `/ua/notebooks/c80004/`.
- Narrowed `popular-search-chips` cleanup so the extension hides only `rz-tag-list` / `.tags-list` blocks instead of the whole page layout.
- Hardened the text-fallback logic for popular search chips to avoid hiding broad category wrappers when Rozetka changes markup.

### Validation
- Added regression tests that verify catalog containers stay visible while popular search chips are still hidden.
- Passed `npm test`
- Passed `npm run test:syntax`
